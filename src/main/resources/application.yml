server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/englishTransfer
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: haicr147258369
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

openai:
  api:
    key: "sk-cuStztjQbInY9aFkK8iUvo89Coq4EybQ5pgIvMbFx5Krb2ge"
    base-url: "https://v98store.com/v1"
    model: "gpt-4o-mini"

app:
  jwt:
    secret: very-secret-key-change-this-in-production-very-secret-key
    expiration-ms: 86400000
  translate:
    google:
      # Put your Google Cloud Translation API key here (or via env var APP_TRANSLATE_GOOGLE_API_KEY)
      api-key: ${APP_TRANSLATE_GOOGLE_API_KEY:}
    ai:
      enabled: false
      url: ${openai.api.base-url:https://api.openai.com/v1}/chat/completions
      api-key: ${openai.api.key:}
      model: ${openai.api.model:gpt-4o-mini}
      vi-to-en-only: false
    libre:
      enabled: true
      url: https://libretranslate.de/translate
    argos:
      enabled: true
      python-command: python
      script-path: tools/argos_translate.py
